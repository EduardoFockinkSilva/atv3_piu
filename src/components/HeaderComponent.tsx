import React from 'react';
import { IonHeader, IonToolbar, IonTitle } from '@ionic/react';

const HeaderComponent: React.FC = () => (
  <IonHeader>
    <IonToolbar>
      <IonTitle>Echoes of Eloquence</IonTitle>
    </IonToolbar>
  </IonHeader>
);

export default HeaderComponent;
